"use client";

// dependencies
// import {} from '';

// @ts-ignore
const Providers = ({ children }): JSX.Element => {
  // TODO: add providers here to wrap around everything
  return <>{children}</>;
};

export default Providers;
